# 進化型AI求人マッチングシステム v3.0

AIとの会話を通じて、ユーザーに最適な求人を見つける次世代マッチングシステム

---

## 🎯 主要機能

### ✅ 1. 基本情報で初期検索（DB検索は1回のみ）
- ユーザーの基本条件（職種、勤務地、年収）で初期検索
- 結果をセッションに保存し、以降はDBアクセスなし
- 高速かつDB負荷を軽減

### ✅ 2. AI動的質問生成
- GPT-4がユーザーごとにパーソナライズした質問を生成
- 会話フェーズに応じて質問タイプを変化
  - フェーズ1（1-3ターン）: 探索（広く聞く）
  - フェーズ2（4-6ターン）: 深掘り（詳しく）
  - フェーズ3（7-10ターン）: 確認（優先順位）

### ✅ 3. 全候補を再スコアリング
- 会話ごとに全候補求人を再評価
- 多様な条件でスコア加算
  - リモートワーク: +20点
  - 学習興味: +15点
  - ワークライフバランス: +10点
  - キャリア成長: +8点
  - キーワードマッチ: +5点
- ターン補正で会話が進むほど精度向上

### ✅ 4. マッチ度0-100%表示
- ターン数に応じた最大スコアで100%換算
- ユーザーにわかりやすいパーセンテージ表示

### ✅ 5. 複数の終了条件
1. **マッチ度80%達成**: 最適な求人が見つかった
2. **スコア収束**: 過去3ターンで±2点以内
3. **ユーザー要求**: 「求人見たい」等の発言
4. **10ターン上限**: 会話が長引きすぎた場合

### ✅ 6. マッチ理由の説明
- GPT-4が各求人のマッチ理由を200-300文字で生成
- 具体的な条件を挙げて説明

### ✅ 7. 完全な追跡システム
- 会話ターンデータ
- スコア履歴
- ユーザー情報の蓄積
- セッションサマリー

---

## 📦 ファイル構成

```
project/
├── main_evolved_complete.py           # メインアプリケーション
├── tracking_evolved.py                # 追跡モジュール
├── dynamic_question_generator_evolved.py  # 質問生成モジュール
├── db_config.py                       # DB接続設定
├── create_tables_evolved.sql          # テーブル作成SQL
├── .env                               # 環境変数
├── requirements.txt                   # 依存パッケージ
└── templates_fastapi/                 # HTMLテンプレート
    ├── landing.html
    ├── form_step1.html
    ├── form_step2.html
    ├── login.html
    ├── profile.html
    └── chat.html
```

---

## 🚀 セットアップ

### 1. 必要なソフトウェア

- Python 3.9+
- PostgreSQL 13+
- OpenAI API キー

### 2. パッケージインストール

```bash
pip install -r requirements.txt
```

**requirements.txt:**
```
fastapi==0.104.1
uvicorn==0.24.0
openai==1.3.0
psycopg2-binary==2.9.9
python-dotenv==1.0.0
werkzeug==3.0.1
jinja2==3.1.2
python-multipart==0.0.6
```

### 3. データベース設定

#### PostgreSQLデータベース作成

```bash
# PostgreSQLにログイン
sudo -u postgres psql

# データベース作成
CREATE DATABASE jobmatch;

# ユーザー作成
CREATE USER devuser WITH PASSWORD 'devpass';

# 権限付与
GRANT ALL PRIVILEGES ON DATABASE jobmatch TO devuser;

# 終了
\q
```

#### テーブル作成

```bash
# 既存テーブル（personal_date, user_profile, company_profile等）が既にある前提
# 進化型システム用の新規テーブルを作成

psql -U devuser -d jobmatch -f create_tables_evolved.sql
```

### 4. 環境変数設定

`.env` ファイルを作成：

```env
# OpenAI API
OPENAI_API_KEY=sk-proj-your-api-key-here

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=jobmatch
DB_USER=devuser
DB_PASSWORD=devpass

# Session
FLASK_SECRET_KEY=your-secret-key-here
```

### 5. 起動

```bash
python main_evolved_complete.py
```

ブラウザで http://localhost:5000 にアクセス

---

## 📊 データベーステーブル

### 既存テーブル（前提）
- `personal_date` - ユーザー基本情報
- `user_profile` - 希望条件
- `company_profile` - 求人情報
- `company_date` - 企業情報

### 新規テーブル（進化型システム用）

#### 1. conversation_turns
各会話ターンのデータを記録

```sql
- turn_number: ターン番号（1-10）
- user_message: ユーザー発言
- bot_message: ボット応答
- extracted_info: AI抽出情報（JSONB）
- top_score: トップスコア
- top_match_percentage: マッチ度（%）
```

#### 2. user_insights
ユーザー情報の蓄積

```sql
- insights: 蓄積された希望・価値観（JSONB）
  - explicit_preferences: 明示的希望
  - implicit_values: 暗黙の価値観
  - pain_points: 不満点
  - keywords: キーワード
```

#### 3. conversation_sessions
セッションの最終結果

```sql
- total_turns: 総ターン数
- end_reason: 終了理由
- final_match_percentage: 最終マッチ度
- presented_jobs: 提示した求人ID（JSONB）
```

#### 4. score_history
スコアの推移を記録

```sql
- turn_number: ターン番号
- job_id: 求人ID
- score: スコア
- match_percentage: マッチ度
- score_details: 加点理由（JSONB）
```

---

## 🎮 使い方

### 1. ユーザー登録

1. トップページにアクセス
2. 「新規登録」をクリック
3. 個人情報を入力（Step 1）
4. 希望条件を入力（Step 2）
   - 職種: 例）デザイナー
   - 勤務地: 例）東京都
   - 希望年収: 例）450万円

### 2. チャット開始

希望条件を入力すると、自動的にチャット画面に遷移

**初回メッセージ例:**
```
こんにちは、田中さん！

デザイナーの求人を43件見つけました。
あなたに最適な求人を見つけるため、いくつか質問させてください。

まず、理想の働き方について教えていただけますか？
例えば、リモートワークやフレックスタイムには興味がありますか？
どんな点に魅力を感じますか？
```

### 3. 会話（最大10ターン）

AIの質問に答えていくと、スコアが更新される

**会話例:**
```
[ターン1]
AI: 理想の働き方について教えてください。
User: リモートワークを希望します。通勤時間が長くて...

→ スコア更新: 20点
→ マッチ度: 67%

[ターン2]
AI: その時間を何に使いたいですか？
User: 家族との時間とReactの勉強です

→ スコア更新: 61.6点
→ マッチ度: 100%

[終了判定: マッチ度80%超え]
```

### 4. 求人提案

終了条件を満たすと、上位5件の求人を提示

**提案メッセージ例:**
```
田中さんに最適なデザイナーの求人が見つかりました！

会話を通じて、以下の希望を理解しました：

✅ リモートワーク（強く希望）
✅ Reactの学習・使用
✅ ワークライフバランス（重視）
✅ 家族との時間確保

厳選した上位5件のデザイナー求人をご紹介します。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【第1位】Webデザイナー
企業名: テック株式会社
マッチ度: 95.2%

【なぜマッチ？】
完全リモートワーク制度により、通勤時間をゼロにして
家族との時間を確保できます。React/Next.jsを使った
モダンな開発環境で、あなたが学びたいと考えている
Reactのスキルを実務で磨けます...

【求人詳細】
📍 勤務地: 東京都（フルリモート可）
💰 年収: 500-700万円
🏠 リモート: 完全リモート可

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
...
```

---

## 🔧 トラブルシューティング

### OpenAI APIエラー

**症状:**
```
❌ 意図抽出エラー: ...
```

**対処法:**
1. `.env` に `OPENAI_API_KEY` が正しく設定されているか確認
2. APIキーの残高を確認
3. レート制限に引っかかっていないか確認

### データベース接続エラー

**症状:**
```
❌ データベース接続エラー: ...
```

**対処法:**
1. PostgreSQLが起動しているか確認
   ```bash
   sudo systemctl status postgresql
   ```
2. `.env` の接続情報を確認
3. ユーザー権限を確認

### テーブルが存在しない

**症状:**
```
relation "conversation_turns" does not exist
```

**対処法:**
```bash
psql -U devuser -d jobmatch -f create_tables_evolved.sql
```

---

## 📈 システム監視

### ログ確認

アプリケーションは標準出力にログを出力：

```
✅ 初期検索: 43件の候補を取得
🔍 抽出情報: {"explicit_preferences": {...}}
📊 ターン1: トップスコア=20.0, マッチ度=67.0%
🤖 生成された質問: その時間を何に使いたいですか？
```

### データベース確認

```sql
-- 最新の会話セッション
SELECT * FROM conversation_sessions 
ORDER BY ended_at DESC 
LIMIT 10;

-- スコア推移
SELECT turn_number, job_id, score, match_percentage
FROM score_history
WHERE session_id = 'your-session-id'
ORDER BY turn_number, score DESC;

-- ユーザーの蓄積情報
SELECT insights FROM user_insights
WHERE user_id = 1
ORDER BY updated_at DESC
LIMIT 1;
```

---

## 🎨 カスタマイズ

### スコアリングルールの調整

`main_evolved_complete.py` の `rescore_all_candidates()` 関数:

```python
# リモートワークの重み変更
if remote_pref in ['強く希望', '希望']:
    if job.get('remote_work') == 'full':
        job['score'] += 30  # 20 → 30 に変更
```

### 終了条件の調整

`should_end_conversation()` 関数:

```python
# マッチ度の閾値変更
if top_match >= 90:  # 80 → 90 に変更
    return {'should_end': True, ...}

# 最大ターン数変更
if turn >= 15:  # 10 → 15 に変更
    return {'should_end': True, ...}
```

### 質問フェーズの調整

`dynamic_question_generator_evolved.py` の `_determine_conversation_phase()`:

```python
def _determine_conversation_phase(self, turn: int) -> str:
    if turn <= 4:  # 3 → 4
        return 'exploration'
    elif turn <= 8:  # 6 → 8
        return 'deepening'
    else:
        return 'confirmation'
```

---

## 📝 ライセンス

MIT License

---

## 👥 貢献

プルリクエスト歓迎！

---

## 📞 サポート

問題が発生した場合は、GitHubのIssuesで報告してください。

---

**進化型AI求人マッチングシステム v3.0 🚀**
